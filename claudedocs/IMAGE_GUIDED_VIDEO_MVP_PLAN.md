# 이미지 가이드 비디오 생성 (MVP) - 계획서

## 개요

### 목표
하나의 이미지를 선택하고, 그 이미지를 어떻게 활용할지 설명하여 기존 프롬프트와 조합함으로써 예측 가능한 비디오를 생성합니다.

### 현재 vs 제안

```
현재:
  텍스트 프롬프트 → VEO → [예측 불가능한 결과]

제안 (MVP):
  텍스트 프롬프트 + 이미지 + 활용 방법 → VEO I2V → [제어 가능한 결과]
```

### 예시 시나리오

```
📷 선택된 이미지: 앨범 커버 이미지

📝 이미지 활용 방법:
"이 앨범 커버가 화면 중앙에서 3D로 회전하며 주변에 빛이 퍼지는 효과"

💬 메인 프롬프트:
"신비로운 분위기의 뮤직비디오 티저"

🔄 조합된 프롬프트:
"신비로운 분위기의 뮤직비디오 티저. Starting from the reference image,
the album cover rotates in 3D at the center with light rays emanating around it.
Cinematic lighting, volumetric effects, smooth camera movement..."

🎬 결과: 앨범 커버가 3D로 회전하는 신비로운 티저 영상
```

---

## 사용자 플로우

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           USER FLOW                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. Generate 페이지 접속                                                │
│         │                                                               │
│         ▼                                                               │
│  2. [이미지 참조 추가] 클릭                                             │
│         │                                                               │
│         ▼                                                               │
│  3. Asset Locker에서 이미지 선택                                        │
│     ┌─────────────────────────────────────┐                            │
│     │ □ 굿즈 이미지      □ 아티스트 사진  │                            │
│     │ □ 앨범 커버        □ 포토카드       │                            │
│     │ □ 업로드 이미지    □ 기타 에셋     │                            │
│     └─────────────────────────────────────┘                            │
│         │                                                               │
│         ▼                                                               │
│  4. 이미지 미리보기 확인                                                │
│     ┌─────────┐                                                        │
│     │  [IMG]  │ album_cover.jpg                                        │
│     └─────────┘                                                        │
│         │                                                               │
│         ▼                                                               │
│  5. "이미지 활용 방법" 입력                                             │
│     "앨범 커버가 3D로 회전하며 빛이 퍼지는 효과"                        │
│         │                                                               │
│         ▼                                                               │
│  6. 메인 프롬프트 입력                                                  │
│     "신비로운 분위기의 뮤직비디오 티저"                                 │
│         │                                                               │
│         ▼                                                               │
│  7. [Generate Video] 클릭                                               │
│         │                                                               │
│         ▼                                                               │
│  8. 프롬프트 자동 조합 → VEO I2V 호출                                   │
│         │                                                               │
│         ▼                                                               │
│  9. 이미지 기반 비디오 생성 완료                                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## UI 디자인

### Generate 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 🎬 Generate Video                                    [Campaign: ...]    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ 📝 프롬프트 *                                                           │
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │ 뮤직비디오 티저 영상을 만들어줘. 신비롭고 몽환적인 분위기로...       │ │
│ │                                                                     │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
│                                              [⚡ AI로 프롬프트 최적화]  │
│                                                                         │
│ ═══════════════════════════════════════════════════════════════════════ │
│                                                                         │
│ 🖼️ 이미지 참조 (선택)                                          [▼ 펼침]│
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │                                                                     │ │
│ │  ┌──────────┐                                                       │ │
│ │  │          │  📁 album_cover.jpg                                   │ │
│ │  │  [IMG]   │  크기: 1024 x 1024                                    │ │
│ │  │  미리보기 │                                                       │ │
│ │  │          │  [🗑️ 제거]  [🔄 다른 이미지 선택]                      │ │
│ │  └──────────┘                                                       │ │
│ │                                                                     │ │
│ │  📋 이미지 활용 방법 *                                              │ │
│ │  ┌─────────────────────────────────────────────────────────────┐   │ │
│ │  │ 이 앨범 커버가 화면 중앙에서 3D로 회전하면서                 │   │ │
│ │  │ 주변에 빛이 퍼져나가는 효과를 넣어줘. 커버 이미지가         │   │ │
│ │  │ 영상의 시작점이 되도록 해줘.                                 │   │ │
│ │  └─────────────────────────────────────────────────────────────┘   │ │
│ │  💡 팁: 이미지가 영상에서 어떻게 나타나고 움직일지 설명해주세요    │ │
│ │                                                                     │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│ 또는                                                                    │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │                                                                     │ │
│ │     [+ Asset Locker에서 이미지 선택]                                │ │
│ │                                                                     │ │
│ │     또는                                                            │ │
│ │                                                                     │ │
│ │     [📤 이미지 업로드]                                              │ │
│ │                                                                     │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│ ═══════════════════════════════════════════════════════════════════════ │
│                                                                         │
│ ⚙️ 생성 설정                                                           │
│                                                                         │
│ Duration        Aspect Ratio        Quality                             │
│ [5초     ▼]    [9:16 (세로) ▼]     [Standard ▼]                        │
│                                                                         │
│ ═══════════════════════════════════════════════════════════════════════ │
│                                                                         │
│                                          [🎬 비디오 생성]               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 이미지 선택 모달

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 🖼️ Asset Locker에서 이미지 선택                                   [X]  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ 🔍 [검색어 입력...]                    카테고리: [전체      ▼]         │
│                                                                         │
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │                                                                     │ │
│ │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐               │ │
│ │  │ [IMG]   │  │ [IMG]   │  │ [IMG]   │  │ [IMG]   │               │ │
│ │  │ 앨범1   │  │ 앨범2   │  │ 포토1   │  │ 포토2   │               │ │
│ │  └─────────┘  └─────────┘  └─────────┘  └─────────┘               │ │
│ │                                                                     │ │
│ │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐               │ │
│ │  │ [IMG]   │  │ [IMG]   │  │ [IMG]   │  │ [IMG]   │               │ │
│ │  │ 굿즈1   │  │ 굿즈2   │  │ 머천1   │  │ 머천2   │               │ │
│ │  └─────────┘  └─────────┘  └─────────┘  └─────────┘               │ │
│ │                                                                     │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│ 선택됨: album_cover.jpg                                                 │
│                                                                         │
│                              [취소]              [✓ 이 이미지 사용]     │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 기술 구현

### 1. 파일 구조

```
components/
└── features/
    └── image-reference/
        ├── image-reference-section.tsx      # 메인 이미지 참조 섹션
        ├── image-picker-modal.tsx           # Asset Locker 이미지 선택 모달
        └── selected-image-card.tsx          # 선택된 이미지 미리보기

lib/
├── image-prompt-combiner.ts                 # 프롬프트 조합 로직 (신규)
└── veo.ts                                   # 기존 I2V 지원 활용

app/
├── (dashboard)/campaigns/[id]/generate/
│   └── page.tsx                             # 수정: 이미지 참조 섹션 추가
└── api/v1/campaigns/[id]/generations/
    └── route.ts                             # 수정: image_description 처리
```

### 2. 데이터 타입

```typescript
// 이미지 참조 상태
interface ImageReference {
  assetId: string;          // Asset Locker의 이미지 ID
  assetUrl: string;         // 이미지 URL
  filename: string;         // 파일명
  description: string;      // 이미지 활용 방법 (사용자 입력)
}

// API 요청 확장
interface CreateVideoGenerationRequest {
  prompt: string;
  negative_prompt?: string;
  duration_seconds?: number;
  aspect_ratio?: string;
  // 기존 필드
  reference_image_id?: string;
  // 새로운 필드
  image_description?: string;        // 이미지 활용 방법
}
```

### 3. 프롬프트 조합 로직

```typescript
// lib/image-prompt-combiner.ts

export interface CombinePromptInput {
  mainPrompt: string;           // 메인 프롬프트 (또는 변환된 veoPrompt)
  imageDescription: string;     // 이미지 활용 방법
  negativePrompt?: string;
}

export interface CombinedPromptOutput {
  combinedPrompt: string;
  enhancedNegativePrompt: string;
}

export function combineWithImageReference(
  input: CombinePromptInput
): CombinedPromptOutput {
  const { mainPrompt, imageDescription, negativePrompt } = input;

  // 이미지 설명을 영어로 최적화 (이미 영어면 유지)
  const imageInstruction = `
Starting from the reference image: ${imageDescription}.
Maintain visual consistency with the reference image throughout the video.
The reference image serves as the starting point and visual anchor.
`.trim();

  // 프롬프트 조합
  const combinedPrompt = `${mainPrompt}. ${imageInstruction}`;

  // Negative prompt 강화 (이미지 왜곡 방지)
  const enhancedNegativePrompt = [
    negativePrompt || "",
    "reference image distortion",
    "inconsistent with source image",
    "identity shift from reference",
    "color palette mismatch",
  ].filter(Boolean).join(", ");

  return {
    combinedPrompt,
    enhancedNegativePrompt,
  };
}
```

### 4. API 수정 (generations route)

```typescript
// app/api/v1/campaigns/[id]/generations/route.ts

// 요청 처리 부분 수정
const {
  prompt,
  negative_prompt,
  duration_seconds = 5,
  aspect_ratio = "16:9",
  reference_image_id,
  image_description,  // 새로운 필드
  // ... 기타 필드
} = body;

// 이미지 참조가 있는 경우 프롬프트 조합
let finalPrompt = prompt;
let finalNegativePrompt = negative_prompt;

if (reference_image_id && image_description) {
  const combined = combineWithImageReference({
    mainPrompt: prompt,
    imageDescription: image_description,
    negativePrompt: negative_prompt,
  });
  finalPrompt = combined.combinedPrompt;
  finalNegativePrompt = combined.enhancedNegativePrompt;
}

// 이미지 Base64 변환
let referenceImageBase64: string | undefined;
if (reference_image_id) {
  const asset = await prisma.asset.findUnique({
    where: { id: reference_image_id },
  });
  if (asset?.s3Url) {
    referenceImageBase64 = await fetchImageAsBase64(asset.s3Url);
  }
}

// VEO 호출
const veoResult = await generateVideo({
  prompt: finalPrompt,
  negativePrompt: finalNegativePrompt,
  referenceImageBase64,  // I2V 모드
  durationSeconds: duration_seconds,
  aspectRatio: aspect_ratio,
});
```

### 5. Generate 페이지 컴포넌트 수정

```typescript
// app/(dashboard)/campaigns/[id]/generate/page.tsx

// 새로운 상태 추가
const [imageReference, setImageReference] = useState<{
  assetId: string;
  assetUrl: string;
  filename: string;
} | null>(null);
const [imageDescription, setImageDescription] = useState("");
const [showImagePicker, setShowImagePicker] = useState(false);

// 생성 요청 수정
const handleGenerate = async () => {
  // ... 기존 검증 로직

  const result = await videoApi.create(campaignId, {
    prompt: promptToUse,
    negative_prompt: negativePrompt,
    duration_seconds: duration,
    aspect_ratio: aspectRatio,
    reference_image_id: imageReference?.assetId,
    image_description: imageDescription || undefined,
    // ... 기타 필드
  });

  // ... 결과 처리
};
```

---

## 구현 일정

### Day 1: UI 컴포넌트 개발

| 작업 | 세부 내용 | 예상 시간 |
|------|-----------|-----------|
| ImageReferenceSection | 이미지 참조 섹션 컴포넌트 | 2시간 |
| SelectedImageCard | 선택된 이미지 미리보기 | 1시간 |
| ImagePickerModal | Asset Locker 이미지 선택 모달 | 2시간 |
| Generate 페이지 통합 | 컴포넌트 통합 및 상태 관리 | 2시간 |

### Day 2: 백엔드 API 개발

| 작업 | 세부 내용 | 예상 시간 |
|------|-----------|-----------|
| image-prompt-combiner.ts | 프롬프트 조합 로직 | 1시간 |
| generations route 수정 | image_description 처리 | 2시간 |
| 이미지 Base64 변환 | S3 → Base64 유틸리티 | 1시간 |
| API 타입 정의 | 요청/응답 타입 업데이트 | 1시간 |

### Day 3: VEO 연동 및 테스트

| 작업 | 세부 내용 | 예상 시간 |
|------|-----------|-----------|
| VEO I2V 테스트 | 이미지 기반 생성 테스트 | 2시간 |
| 프롬프트 조합 품질 확인 | 다양한 조합 테스트 | 2시간 |
| 에러 핸들링 | 실패 케이스 처리 | 1시간 |
| 디버깅 | 이슈 수정 | 2시간 |

### Day 4-5: 통합 및 폴리싱

| 작업 | 세부 내용 | 예상 시간 |
|------|-----------|-----------|
| The Bridge 연동 | 이미지 참조 전달 플로우 | 2시간 |
| UI/UX 개선 | 로딩 상태, 애니메이션 | 2시간 |
| 반응형 디자인 | 모바일 대응 | 1시간 |
| 테스트 및 버그 수정 | 통합 테스트 | 3시간 |
| 문서화 | 사용자 가이드 | 1시간 |

---

## 성공 지표

### 정량적 지표

| 지표 | 현재 | 목표 |
|------|------|------|
| 재생성 횟수 (이미지 참조 사용 시) | N/A | 1-2회 |
| 이미지 참조 기능 사용률 | 0% | 30%+ |
| 생성 결과 만족도 | 60% | 75%+ |

### 정성적 지표

- 사용자가 "이미지대로 나왔다"고 느끼는가
- 굿즈/상품 이미지가 영상에 잘 반영되는가
- 이미지 활용 방법 입력이 직관적인가

---

## 리스크 및 대응

| 리스크 | 가능성 | 영향 | 대응 |
|--------|--------|------|------|
| VEO I2V 품질 불안정 | 중 | 높음 | 프롬프트 최적화, 재생성 옵션 |
| 이미지-프롬프트 불일치 | 중 | 중 | 가이드 문구, 예시 제공 |
| 이미지 로딩 지연 | 낮음 | 낮음 | 이미지 최적화, 로딩 표시 |

---

## 향후 확장 (장기 VSS 연결)

이 MVP가 완성되면 다음 단계로 확장 가능:

1. **다중 이미지 지원** → 키프레임 시스템의 기반
2. **이미지 자동 생성** → Imagen 3 연동
3. **세그먼트 기반 생성** → 키프레임 간 비디오 생성
4. **스토리보드 캔버스** → 전체 VSS 구현

```
MVP (현재)                    확장 (향후)
─────────────                 ─────────────
1개 이미지 선택      →        3-5개 키프레임 선택
이미지 활용 방법     →        각 키프레임 설명
프롬프트 조합        →        세그먼트별 프롬프트
VEO I2V 생성        →        세그먼트 생성 + 조합
```

---

*이 문서는 이미지 가이드 비디오 생성 MVP의 계획서입니다.*

**작성일:** 2024-11-28
**예상 소요:** 3-5일
**우선순위:** 높음 (즉시 구현 가능)
