# Campaign UX 구조 개선 계획

## 현재 구조 분석

### 파일 구조
```
app/(dashboard)/campaigns/
├── page.tsx                    # 캠페인 리스트
├── new/page.tsx                # 새 캠페인 생성
└── [id]/
    ├── layout.tsx              # 탭 레이아웃 (에셋|생성|컴포즈|영상|발행|분석)
    ├── page.tsx                # 에셋 관리 페이지 (현재 기본 페이지)
    ├── generate/page.tsx       # AI 영상 생성
    ├── compose/page.tsx        # 이미지 슬라이드쇼 제작
    ├── curation/page.tsx       # 영상 큐레이션
    ├── publish/page.tsx        # 발행
    └── analytics/page.tsx      # 분석
```

### 현재 문제점

1. **탭 구조 혼란**
   - "생성" (AI 영상 생성)과 "컴포즈" (슬라이드쇼)가 별도 탭
   - 사용자가 어떤 것을 선택해야 하는지 헷갈림

2. **상세보기 동작 오류**
   - 리스트에서 Eye 아이콘 클릭 → 에셋 관리 페이지로 이동
   - 기대 동작: 캠페인 정보 조회/수정 페이지

3. **컴포즈 이미지 소스 제한**
   - 현재: 구글 검색 이미지만 사용 가능
   - 원하는: 업로드된 에셋 이미지도 사용 가능

---

## 개선 계획

### Phase 1: 캠페인 상세 정보 페이지 분리

#### 새 파일 생성: `/campaigns/[id]/info/page.tsx`
- 캠페인 기본 정보 표시 및 수정
  - 이름, 설명
  - 상태 (Draft/Active/Completed/Archived)
  - 시작일/종료일
  - 아티스트 정보
- 캠페인 통계 요약
  - 총 에셋 수
  - 생성된 영상 수
  - 발행된 영상 수

#### campaigns/page.tsx 수정
```tsx
// Before
<Link href={`/campaigns/${campaign.id}`}>

// After
<Link href={`/campaigns/${campaign.id}/info`}>
```

### Phase 2: 탭 구조 변경

#### 현재 탭
```
에셋 | 생성 | 컴포즈 | 영상 | 발행 | 분석
```

#### 변경 후 탭
```
에셋 | 만들기 | 영상 | 발행 | 분석
```

#### layout.tsx 수정
```tsx
const tabs: TabConfig[] = [
  { id: "assets", icon: FolderOpen, path: "" },
  { id: "create", icon: Sparkles, path: "/create" },  // 통합
  { id: "videos", icon: Video, path: "/curation" },
  { id: "publish", icon: Send, path: "/publish" },
  { id: "analytics", icon: BarChart3, path: "/analytics" },
];
```

### Phase 3: 만들기 페이지 생성

#### 새 파일: `/campaigns/[id]/create/page.tsx`

**UI 구조:**
```
┌─────────────────────────────────────────────────────┐
│                   만들기                              │
│                                                      │
│  ┌──────────────────┐    ┌──────────────────┐       │
│  │   ✨ AI 생성     │    │   🖼 이미지 합성  │       │
│  │                  │    │                  │       │
│  │  AI가 프롬프트    │    │  이미지들을 조합   │       │
│  │  기반으로 새로운   │    │  하여 슬라이드쇼   │       │
│  │  영상을 생성합니다 │    │  영상을 제작합니다 │       │
│  │                  │    │                  │       │
│  │   [시작하기]      │    │   [시작하기]      │       │
│  └──────────────────┘    └──────────────────┘       │
│                                                      │
└─────────────────────────────────────────────────────┘
```

**선택 후 동작:**
- AI 생성 선택 → Generate 페이지 내용 표시
- 이미지 합성 선택 → Compose 페이지 내용 표시

### Phase 4: 컴포즈에 에셋 이미지 기능 추가

#### compose/page.tsx 수정

**Step 2 (이미지 선택) 개선:**

```
┌─────────────────────────────────────────────────────────┐
│  이미지 소스 선택                                        │
│                                                         │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │ 에셋만      │ │ 검색만      │ │ 둘 다 혼합   │       │
│  └─────────────┘ └─────────────┘ └─────────────┘       │
│                                                         │
│  ─────────────────────────────────────────────────────  │
│                                                         │
│  📁 캠페인 에셋 이미지 (12개)                            │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐            │
│  │ ✓  │ │    │ │ ✓  │ │    │ │    │ │ ✓  │            │
│  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘            │
│                                                         │
│  🔍 검색 이미지 (키워드: kpop, concert)                  │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐            │
│  │    │ │ ✓  │ │    │ │ ✓  │ │    │ │    │            │
│  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘            │
│                                                         │
│  선택됨: 5/10                                           │
└─────────────────────────────────────────────────────────┘
```

**새로운 State:**
```tsx
type ImageSourceMode = "assets_only" | "search_only" | "mixed";

const [imageSourceMode, setImageSourceMode] = useState<ImageSourceMode>("mixed");
const [assetImages, setAssetImages] = useState<Asset[]>([]);
const [selectedAssetImages, setSelectedAssetImages] = useState<Asset[]>([]);
```

**API 호출:**
```tsx
// 캠페인 에셋 이미지 로드
const loadAssetImages = async () => {
  const result = await assetsApi.getByCampaign(campaignId, {
    type: "image",
    page_size: 50,
  });
  if (result.data) {
    setAssetImages(result.data.items);
  }
};
```

---

## 파일 변경 목록

### 새로 생성할 파일
1. `app/(dashboard)/campaigns/[id]/info/page.tsx` - 캠페인 상세 정보 페이지
2. `app/(dashboard)/campaigns/[id]/create/page.tsx` - 만들기 선택 페이지

### 수정할 파일
1. `app/(dashboard)/campaigns/page.tsx` - 상세보기 링크 변경
2. `app/(dashboard)/campaigns/[id]/layout.tsx` - 탭 구조 변경
3. `app/(dashboard)/campaigns/[id]/compose/page.tsx` - 에셋 이미지 기능 추가
4. `lib/i18n/index.ts` - 새로운 번역 키 추가
5. `lib/stores/ui-store.ts` - CampaignTab 타입 수정

### 삭제/이동할 파일
- 기존 `generate/page.tsx`와 `compose/page.tsx`는 유지
- `create/page.tsx`에서 import하여 조건부 렌더링

---

## UI/UX 플로우

### 캠페인 리스트 → 상세보기
```
[캠페인 리스트]
     ↓ Eye 아이콘 클릭
[캠페인 상세 정보 페이지] (/campaigns/[id]/info)
     │
     ├─ 정보 수정 가능
     └─ 탭으로 다른 기능 접근
```

### 만들기 플로우
```
[만들기 탭 클릭] (/campaigns/[id]/create)
     │
     ├─ AI 생성 선택 → Generate UI 표시
     │                 └─ 프롬프트 입력 → 영상 생성
     │
     └─ 이미지 합성 선택 → Compose UI 표시
                          └─ Step 1: 스크립트
                          └─ Step 2: 이미지 (에셋/검색/혼합)
                          └─ Step 3: 음악
                          └─ Step 4: 렌더링
```

---

## 구현 순서

### 1단계: 기반 작업
- [ ] 캠페인 상세 정보 페이지 생성
- [ ] 리스트 링크 수정

### 2단계: 탭 통합
- [ ] layout.tsx 탭 구조 변경
- [ ] 만들기 페이지 생성 (선택 UI)
- [ ] 기존 generate/compose 컴포넌트화

### 3단계: 컴포즈 개선
- [ ] 에셋 이미지 로드 기능
- [ ] 이미지 소스 모드 선택 UI
- [ ] 에셋/검색 이미지 혼합 선택

### 4단계: 마무리
- [ ] 번역 추가
- [ ] 테스트
- [ ] 기존 URL 리다이렉트 처리

---

## 예상 소요 시간

| 단계 | 작업 | 복잡도 |
|------|------|--------|
| 1단계 | 상세 정보 페이지 | 중 |
| 2단계 | 탭 통합 | 상 |
| 3단계 | 컴포즈 개선 | 상 |
| 4단계 | 마무리 | 하 |

---

## 질문 사항

구현 전 확인이 필요한 사항:

1. **만들기 페이지 동작 방식**
   - Option A: 선택 후 같은 페이지에서 UI 변경 (SPA 스타일)
   - Option B: 선택 후 `/create/generate` 또는 `/create/compose`로 이동

2. **기존 URL 호환성**
   - `/campaigns/[id]/generate`와 `/campaigns/[id]/compose` URL 유지할지?
   - 리다이렉트 처리 필요한지?

3. **에셋 이미지 선택 시 동작**
   - 에셋 이미지도 프록시 처리 필요한지?
   - 아니면 S3 URL 직접 사용 가능한지?
