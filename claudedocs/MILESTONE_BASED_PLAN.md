# HYBE HYDRA - 마일스톤 기반 개발 계획

**목적**: 각 마일스톤별로 독립적으로 테스트 가능한 기능 단위로 개발 진행

---

## 마일스톤 개요

```
M1 → M2 → M3 → M4 → M5 → M6 → M7 → M8 → M9 → M10
│    │    │    │    │    │    │    │    │    │
▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼
인프라 인증  에셋   Veo3  프롬프트 병렬   스코어링 큐레이션 모션    퍼블리싱
설정  시스템 업로드 단일   연금술  생성   시스템   UI     트랜스퍼
                  생성
```

---

## M1: 프로젝트 인프라 설정

### 목표
개발 환경 구축 및 기본 프로젝트 구조 설정

### 범위
```
□ 모노레포 구조 설정 (frontend/, backend/)
□ Docker Compose 환경 (PostgreSQL, Redis, MinIO)
□ 기본 Next.js 앱 구조
□ 기본 FastAPI 앱 구조
□ 환경 변수 관리 (.env)
```

### 테스트 체크포인트
```bash
# 1. Docker 환경 실행
docker-compose up -d

# 2. Frontend 실행 확인
cd frontend && npm run dev
# → http://localhost:3000 접속 가능

# 3. Backend 실행 확인
cd backend && uvicorn app.main:app --reload
# → http://localhost:8000/docs 접속 가능 (Swagger UI)

# 4. DB 연결 확인
curl http://localhost:8000/health
# → {"status": "ok", "database": "connected"}
```

### 산출물
- `/docker-compose.yml`
- `/frontend/` (Next.js 기본 구조)
- `/backend/` (FastAPI 기본 구조)
- `GET /health` API

---

## M2: 인증 시스템

### 목표
사용자 로그인 및 RBAC 기반 접근 제어

### 범위
```
□ PostgreSQL Users 테이블
□ JWT 토큰 발급/검증
□ 로그인 API (간단한 이메일/패스워드 또는 Mock OAuth)
□ RBAC: Admin, Producer, Viewer 역할
□ 프론트엔드 로그인 페이지
□ 인증 상태 관리 (Zustand)
```

### 테스트 체크포인트
```bash
# 1. 회원가입/로그인 API
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email": "test@hybe.com", "password": "test123", "name": "Test User"}'
# → {"access_token": "eyJ...", "user": {...}}

# 2. 보호된 API 접근
curl http://localhost:8000/api/v1/users/me \
  -H "Authorization: Bearer eyJ..."
# → {"id": "uuid", "email": "test@hybe.com", "role": "producer"}

# 3. 프론트엔드 로그인 플로우
# → 로그인 페이지에서 로그인 → 대시보드 리다이렉트
```

### 산출물
- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `GET /api/v1/users/me`
- 로그인 페이지 UI

---

## M3: 캠페인 & 에셋 업로드

### 목표
캠페인 생성 및 Asset Locker 기본 기능 (파일 업로드)

### 범위
```
□ Campaigns, Artists, Assets 테이블
□ 캠페인 CRUD API
□ 파일 업로드 API (S3/MinIO)
□ 이미지/비디오/오디오 타입 검증
□ 캠페인 목록/생성 UI
□ 에셋 업로드 UI (드래그 앤 드롭)
□ 에셋 갤러리 뷰
```

### 테스트 체크포인트
```bash
# 1. 캠페인 생성
curl -X POST http://localhost:8000/api/v1/campaigns \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "BTS Dynamite Campaign", "artist_id": "uuid"}'
# → {"id": "uuid", "name": "BTS Dynamite Campaign", ...}

# 2. 에셋 업로드
curl -X POST http://localhost:8000/api/v1/campaigns/{id}/assets \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@jungkook.jpg" \
  -F "type=image"
# → {"id": "uuid", "s3_url": "http://minio:9000/...", "type": "image"}

# 3. 에셋 목록 조회
curl http://localhost:8000/api/v1/campaigns/{id}/assets \
  -H "Authorization: Bearer $TOKEN"
# → [{"id": "...", "filename": "jungkook.jpg", ...}, ...]

# 4. UI 테스트
# → 캠페인 생성 → 에셋 업로드 → 갤러리에서 확인
```

### 산출물
- Campaign/Asset CRUD APIs
- 파일 업로드 → S3 저장 파이프라인
- 캠페인 관리 UI
- Asset Locker 갤러리 UI

---

## M4: Vector Embedding (RAG 기반)

### 목표
업로드된 에셋을 Vector DB에 임베딩하여 검색 가능하게 함

### 범위
```
□ Vertex AI Embedding API 연동
□ Pinecone/Milvus 연동
□ 에셋 업로드 시 자동 임베딩 (Celery Task)
□ 벡터 유사도 검색 API
□ 아티스트 프로필 자동 생성
```

### 테스트 체크포인트
```bash
# 1. 에셋 업로드 후 임베딩 확인
curl -X POST http://localhost:8000/api/v1/campaigns/{id}/assets \
  -F "file=@jungkook.jpg"
# → 업로드 후 vector_embedding_id 생성됨

# 2. 벡터 검색 테스트
curl -X POST http://localhost:8000/api/v1/assets/search \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query": "dark hair male idol", "campaign_id": "uuid", "top_k": 5}'
# → [{"id": "...", "similarity": 0.92, ...}, ...]

# 3. 아티스트 프로필 조회
curl http://localhost:8000/api/v1/artists/{id}/profile
# → {"visual_description": "Dark brown wavy hair, clean-shaven...", ...}
```

### 산출물
- Embedding 파이프라인 (Celery Worker)
- `POST /api/v1/assets/search` (벡터 검색)
- `GET /api/v1/artists/{id}/profile` (동적 프로필)

---

## M5: Veo 3 단일 영상 생성

### 목표
단순 프롬프트 입력 → Veo 3 API → 단일 영상 생성

### 범위
```
□ Vertex AI Veo 3 API 클라이언트
□ 프로젝트(Prompt) 생성 API
□ 영상 생성 요청 API
□ 비동기 작업 상태 조회 (Celery)
□ 생성된 영상 S3 저장
□ 프롬프트 입력 UI
□ 생성 상태 표시 UI
□ 결과 영상 재생 UI
```

### 테스트 체크포인트
```bash
# 1. 프로젝트 생성
curl -X POST http://localhost:8000/api/v1/projects \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "campaign_id": "uuid",
    "user_input": "Jungkook dancing in the rain, cinematic"
  }'
# → {"id": "uuid", "status": "draft", ...}

# 2. 영상 생성 시작
curl -X POST http://localhost:8000/api/v1/projects/{id}/generate \
  -H "Authorization: Bearer $TOKEN"
# → {"job_id": "uuid", "status": "processing"}

# 3. 상태 조회 (폴링)
curl http://localhost:8000/api/v1/projects/{id}/status
# → {"status": "completed", "progress": 100, "s3_url": "..."}

# 4. UI 테스트
# → 프롬프트 입력 → 생성 시작 → 로딩 → 결과 영상 재생
```

### 산출물
- Veo 3 API 클라이언트
- Project/VideoVariant 모델
- 생성 Celery Task
- 프롬프트 입력 & 결과 뷰어 UI

**이 마일스톤 완료 시: MVP 핵심 기능 동작 확인 가능**

---

## M6: Prompt Alchemist (LLM 프롬프트 최적화)

### 목표
사용자의 간단한 입력을 Veo 3 최적화 프롬프트로 변환

### 범위
```
□ Gemini Pro API 연동
□ System Prompt 템플릿 관리
□ Safety Filter (금지어 탐지)
□ 프롬프트 변환 API
□ Asset Locker RAG 연동 (아티스트 프로필 주입)
□ Negative Prompt 자동 생성
□ 변환 미리보기 UI
□ 프롬프트 승인 플로우
```

### 테스트 체크포인트
```bash
# 1. 프롬프트 변환
curl -X POST http://localhost:8000/api/v1/prompts/transform \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "user_input": "정국, 슬픈 춤, 비",
    "campaign_id": "uuid"
  }'
# → {
#   "status": "success",
#   "veo_prompt": "Cinematic 4K video of Jeon Jungkook with dark brown wavy hair...",
#   "negative_prompt": "distortion, bad anatomy, morphing...",
#   "technical_settings": {"aspect_ratio": "9:16", "fps": 60}
# }

# 2. Safety Filter 테스트
curl -X POST http://localhost:8000/api/v1/prompts/transform \
  -d '{"user_input": "아티스트가 담배 피우는 장면", "campaign_id": "uuid"}'
# → {"status": "blocked", "reason": "Brand Safety 위반..."}

# 3. UI 테스트
# → 간단 입력 → 변환된 프롬프트 미리보기 → 승인 → 생성
```

### 산출물
- Prompt Alchemist 서비스 (LangChain)
- `POST /api/v1/prompts/transform`
- Safety Filter
- 프롬프트 미리보기/승인 UI

---

## M7: 병렬 생성 엔진 (1:15)

### 목표
하나의 프롬프트로 15개의 스타일 변형 영상 동시 생성

### 범위
```
□ Style Preset 시스템 (10-15개 프리셋)
□ Celery Task Group (병렬 처리)
□ 배치 생성 API
□ 실시간 진행 상태 (WebSocket)
□ 부분 성공 처리
□ 생성 진행률 UI
□ 다중 결과 썸네일 표시
```

### 테스트 체크포인트
```bash
# 1. 스타일 프리셋 조회
curl http://localhost:8000/api/v1/presets
# → [{"id": "...", "name": "High Contrast"}, {"name": "Soft/Pastel"}, ...]

# 2. 배치 생성 시작
curl -X POST http://localhost:8000/api/v1/projects/{id}/generate-batch \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"preset_ids": ["uuid1", "uuid2", ..., "uuid15"]}'
# → {"job_group_id": "uuid", "variants": [{"id": "...", "status": "queued"}, ...]}

# 3. WebSocket 실시간 상태
wscat -c ws://localhost:8000/ws/projects/{id}/status
# → {"type": "variant_update", "variant_id": "...", "status": "completed", "progress": 100}

# 4. 전체 결과 조회
curl http://localhost:8000/api/v1/projects/{id}/variants
# → 15개 영상 목록 (일부 completed, 일부 failed 가능)

# 5. UI 테스트
# → 배치 생성 → 실시간 진행률 바 → 완료된 것부터 썸네일 표시
```

### 산출물
- Style Preset 관리
- 병렬 생성 Celery Task Group
- WebSocket 실시간 상태
- 다중 결과 UI

---

## M8: AI Scoring 시스템

### 목표
생성된 영상에 바이럴 가능성 점수 부여

### 범위
```
□ Feature 추출 (조도, 색상, 얼굴 인식률)
□ 스코어링 모델 (가중치 기반)
□ 점수 계산 API
□ 일괄 점수 계산
□ 점수 기반 정렬
□ 점수 표시 UI (차트)
```

### 테스트 체크포인트
```bash
# 1. 단일 영상 점수 계산
curl -X POST http://localhost:8000/api/v1/variants/{id}/score \
  -H "Authorization: Bearer $TOKEN"
# → {
#   "ai_score": 87.5,
#   "breakdown": {
#     "trend_similarity": 0.85,
#     "brand_fit": 0.92,
#     "visual_quality": 0.88
#   }
# }

# 2. 프로젝트 전체 점수 계산
curl -X POST http://localhost:8000/api/v1/projects/{id}/score-all \
  -H "Authorization: Bearer $TOKEN"
# → {"scored_count": 15, "average_score": 78.3}

# 3. 점수순 정렬 조회
curl "http://localhost:8000/api/v1/projects/{id}/variants?sort=ai_score&order=desc"
# → 점수 높은 순으로 정렬된 목록

# 4. UI 테스트
# → 각 영상 썸네일에 점수 표시 → 상세 점수 breakdown 차트
```

### 산출물
- Scoring 서비스
- `POST /api/v1/variants/{id}/score`
- 점수 breakdown 시각화

---

## M9: 큐레이션 대시보드

### 목표
사용자가 15개 영상을 효율적으로 비교/선택할 수 있는 UI

### 범위
```
□ 5x3 모자이크 뷰어 (동시 재생)
□ 마우스 오버 오디오 활성화
□ A/B 비교 모달
□ Refine (수정 요청) 기능
□ 승인/삭제 액션
□ 영상 상세 모달
```

### 테스트 체크포인트
```
# UI 테스트 시나리오

1. 모자이크 뷰어
   → 15개 영상이 5x3 그리드로 표시
   → 모든 영상 동시 재생 (음소거)
   → 마우스 오버 시 해당 영상 오디오 ON

2. 영상 선택 & 비교
   → 2개 영상 선택 → "Compare" 버튼
   → A/B 나란히 재생 모달
   → 점수 비교 테이블

3. Refine 기능
   → 영상 선택 → "Refine" 버튼
   → "배경을 밤으로 바꿔줘" 입력
   → 새로운 Variant 생성

4. 승인 플로우
   → 영상 선택 → "Approve" 버튼
   → 승인된 영상 표시 (체크 아이콘)
```

### 산출물
- Mosaic Viewer 컴포넌트
- A/B Comparison Modal
- Refine API 및 UI
- 승인 플로우

---

## M10: 퍼블리싱 & 스케줄링

### 목표
승인된 영상을 YouTube/TikTok에 예약 게시

### 범위
```
□ Smart Crop (9:16 변환)
□ 캡션/해시태그 자동 생성
□ YouTube API 연동
□ TikTok API 연동
□ 예약 포스팅 스케줄러
□ 퍼블리싱 UI
```

### 테스트 체크포인트
```bash
# 1. Smart Crop
curl -X POST http://localhost:8000/api/v1/variants/{id}/crop \
  -H "Authorization: Bearer $TOKEN"
# → {"cropped_url": "...", "aspect_ratio": "9:16"}

# 2. 캡션 생성
curl -X POST http://localhost:8000/api/v1/variants/{id}/caption \
  -H "Authorization: Bearer $TOKEN"
# → {
#   "caption": "비 오는 거리에서 펼쳐지는 감동의 퍼포먼스 💜 이 순간을 놓치지 마세요!",
#   "hashtags": ["#정국", "#BTS", "#춤", "#감성", "#숏츠"]
# }

# 3. 게시 예약
curl -X POST http://localhost:8000/api/v1/variants/{id}/schedule \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "platforms": ["youtube", "tiktok"],
    "scheduled_at": "2024-12-01T18:00:00Z",
    "caption": "...",
    "hashtags": [...]
  }'
# → {"schedule_id": "uuid", "status": "scheduled"}

# 4. UI 테스트
# → 영상 선택 → 크롭 미리보기 → 캡션 편집 → 플랫폼 선택 → 예약
```

### 산출물
- Smart Crop 서비스
- Caption Generator
- SNS API 연동
- Publishing Scheduler UI

---

## 마일스톤별 예상 소요 기간

| Milestone | 기능 | 기간 | 누적 |
|-----------|------|------|------|
| M1 | 인프라 설정 | 3일 | 3일 |
| M2 | 인증 시스템 | 4일 | 1주 |
| M3 | 캠페인 & 에셋 업로드 | 5일 | 2주 |
| M4 | Vector Embedding | 4일 | 2.5주 |
| M5 | Veo 3 단일 생성 | 5일 | 3.5주 |
| M6 | Prompt Alchemist | 5일 | 4.5주 |
| M7 | 병렬 생성 (1:15) | 5일 | 5.5주 |
| M8 | AI Scoring | 4일 | 6.5주 |
| M9 | 큐레이션 대시보드 | 5일 | 7.5주 |
| M10 | 퍼블리싱 | 5일 | 8.5주 |

**총 예상 기간: 약 8-9주 (2개월)**

---

## 테스트 가능 범위 요약

```
┌─────────────────────────────────────────────────────────────────┐
│  M1-M2: 기본 인프라 + 로그인                                     │
│  테스트: 서버 실행, DB 연결, 로그인/로그아웃                      │
├─────────────────────────────────────────────────────────────────┤
│  M3-M4: Asset Locker                                            │
│  테스트: 파일 업로드 → S3 저장 → 임베딩 → 벡터 검색              │
├─────────────────────────────────────────────────────────────────┤
│  M5: 핵심 MVP ⭐                                                 │
│  테스트: 프롬프트 입력 → Veo 3 호출 → 단일 영상 생성 → 재생      │
├─────────────────────────────────────────────────────────────────┤
│  M6: 프롬프트 최적화                                             │
│  테스트: 간단 입력 → LLM 변환 → 최적화 프롬프트 + Safety 필터    │
├─────────────────────────────────────────────────────────────────┤
│  M7: 대량 생성                                                   │
│  테스트: 1개 프롬프트 → 15개 스타일 변형 동시 생성               │
├─────────────────────────────────────────────────────────────────┤
│  M8-M9: 큐레이션                                                 │
│  테스트: 점수 계산 → 모자이크 뷰 → A/B 비교 → 선택/승인          │
├─────────────────────────────────────────────────────────────────┤
│  M10: 배포                                                       │
│  테스트: 크롭 → 캡션 → SNS 예약 포스팅                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 권장 개발 순서

### 최소 동작 확인 (M1 → M5)
```
M1 (인프라) → M2 (인증) → M3 (에셋) → M5 (Veo 3 단일 생성)
```
**결과**: 로그인 → 캠페인 생성 → 프롬프트 입력 → 영상 1개 생성

### 핵심 기능 완성 (+ M4, M6, M7)
```
+ M4 (Embedding) → M6 (Prompt Alchemist) → M7 (병렬 생성)
```
**결과**: RAG 기반 프롬프트 최적화 → 15개 영상 동시 생성

### 전체 플로우 완성 (+ M8, M9, M10)
```
+ M8 (Scoring) → M9 (큐레이션) → M10 (퍼블리싱)
```
**결과**: 점수 기반 선택 → SNS 자동 게시

---

## 각 마일스톤 시작 전 확인사항

| Milestone | 선행 조건 |
|-----------|----------|
| M1 | 없음 |
| M2 | M1 완료 (Docker, DB 실행) |
| M3 | M2 완료 (인증 토큰 발급) |
| M4 | M3 완료 (에셋 업로드), Vertex AI 계정 |
| M5 | M3 완료, Veo 3 API 액세스 |
| M6 | M4 완료 (벡터 검색), Gemini API 액세스 |
| M7 | M5 완료 (단일 생성 동작) |
| M8 | M7 완료 (영상 생성 완료) |
| M9 | M8 완료 (점수 계산) |
| M10 | M9 완료, YouTube/TikTok API 액세스 |
