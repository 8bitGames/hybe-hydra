# Visual Storyboard Studio (VSS) - 프로젝트 계획서

## 개요

### 문제점
현재 HYBE Hydra의 비디오 생성 워크플로우는 **텍스트 프롬프트 → 비디오**의 단선적 구조로, 결과물의 예측이 어렵고 사용자가 원하는 영상을 얻기까지 많은 시행착오가 필요합니다.

### 제안 솔루션
**키프레임 기반 비디오 생성 시스템** - 이미지(시작, 중간, 끝 프레임)를 통해 비디오 생성을 가이드하여 예측 가능하고 제어 가능한 결과물을 생성합니다.

### 핵심 가치
```
기존: 텍스트 프롬프트 → [블랙박스] → 예측 불가능한 비디오
제안: 컨셉 → 키프레임 이미지 → 비디오 세그먼트 → 조합된 비디오
                ↑ 사용자 제어 가능
```

---

## 시스템 아키텍처

### 전체 워크플로우

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        VISUAL STORYBOARD STUDIO                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. CONCEPT INPUT                                                           │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                     │
│  │ 사용자 입력  │    │ 아티스트    │    │ 굿즈/상품   │                     │
│  │ (텍스트)    │ +  │ 사진 참조   │ +  │ 이미지     │                     │
│  └─────────────┘    └─────────────┘    └─────────────┘                     │
│         │                  │                  │                             │
│         └──────────────────┼──────────────────┘                             │
│                            ▼                                                │
│  2. KEYFRAME GENERATION                                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  [시작 프레임]  ─────  [중간 프레임]  ─────  [끝 프레임]              │   │
│  │      KF1                  KF2                  KF3                   │   │
│  │   (Imagen 3)           (Imagen 3)           (Imagen 3)               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│         │                    │                    │                         │
│         ▼                    ▼                    ▼                         │
│  3. SEGMENT GENERATION (VEO Image-to-Video)                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  [Segment 1]          [Segment 2]          [Segment 3]               │   │
│  │   KF1 → KF2            KF2 → KF3            KF3 (hold)               │   │
│  │   (5초 영상)           (5초 영상)           (5초 영상)                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│         │                    │                    │                         │
│         └────────────────────┼────────────────────┘                         │
│                              ▼                                              │
│  4. COMPOSITION                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  [Seg1] ──transition── [Seg2] ──transition── [Seg3]                  │   │
│  │                     + Audio Track                                    │   │
│  │                     = Final Video (15초)                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 현재 시스템과의 통합

```
┌──────────────────────────────────────────────────────────────────────────┐
│                      HYBE HYDRA INTEGRATION MAP                          │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────┐         ┌─────────────┐         ┌─────────────┐        │
│  │  Campaign   │ ──────▶ │  The Bridge │ ──────▶ │   VSS       │        │
│  │  Selection  │         │  (트렌드)    │         │ (스토리보드) │        │
│  └─────────────┘         └─────────────┘         └─────────────┘        │
│         │                       │                       │                │
│         ▼                       ▼                       ▼                │
│  ┌─────────────┐         ┌─────────────┐         ┌─────────────┐        │
│  │ Asset       │ ◀─────▶ │ Prompt      │ ◀─────▶ │ Keyframe    │        │
│  │ Locker      │         │ Alchemist   │         │ Generator   │        │
│  │ (이미지/굿즈)│         │ (프롬프트)   │         │ (Imagen 3)  │        │
│  └─────────────┘         └─────────────┘         └─────────────┘        │
│         │                       │                       │                │
│         └───────────────────────┼───────────────────────┘                │
│                                 ▼                                        │
│                          ┌─────────────┐                                 │
│                          │  VEO 3.1    │                                 │
│                          │  (I2V Gen)  │                                 │
│                          └─────────────┘                                 │
│                                 │                                        │
│                                 ▼                                        │
│                          ┌─────────────┐                                 │
│                          │ Composition │                                 │
│                          │ (FFmpeg)    │                                 │
│                          └─────────────┘                                 │
│                                 │                                        │
│                                 ▼                                        │
│                          ┌─────────────┐                                 │
│                          │ Publishing  │                                 │
│                          │ Pipeline    │                                 │
│                          └─────────────┘                                 │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## UI/UX 설계

### Storyboard Canvas 인터페이스

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 📽️ Storyboard: "Thomas Rhett - About A Woman Campaign"        [Save] [Exit]│
├──────────────┬──────────────────────────────────────────┬───────────────────┤
│              │                                          │                   │
│ 📁 ASSETS    │           🎬 TIMELINE CANVAS             │ ⚙️ PROPERTIES     │
│              │                                          │                   │
│ ▼ 아티스트    │  0s      5s      10s     15s     20s    │ Selected: KF2     │
│   □ 프로필1   │  │       │       │       │       │     │ ┌───────────────┐ │
│   □ 프로필2   │  ┌───┐   ┌───┐   ┌───┐   ┌───┐   ┌───┐ │ │               │ │
│   □ 라이브    │  │KF1│───│KF2│───│KF3│───│KF4│───│KF5│ │ │   [Preview]   │ │
│              │  └───┘   └───┘   └───┘   └───┘   └───┘ │ │               │ │
│ ▼ 굿즈       │    │       │       │       │       │   │ └───────────────┘ │
│   □ 앨범커버  │    ▼       ▼       ▼       ▼       ▼   │                   │
│   □ 포토카드  │  [S1]    [S2]    [S3]    [S4]    [S5]  │ Position: 25%     │
│   □ 머천다이즈 │  ████    ████    ████    ████    ████  │ Duration: 5s      │
│              │                                          │ Transition: Fade  │
│ ▼ 스타일참조  │  ─────────────────────────────────────  │                   │
│   □ 뮤비참조1 │  🎵 Audio: "About A Woman" (0:00-0:20)  │ Style Reference:  │
│   □ 뮤비참조2 │                                          │ [+ Add]           │
│              │  ┌────────────────────────────────────┐  │                   │
│ [+ Upload]   │  │  ▶ PREVIEW    ⚡ GENERATE ALL     │  │ [🔄 Regenerate]  │
│ [🔍 Search]  │  └────────────────────────────────────┘  │ [🗑️ Delete]      │
│              │                                          │                   │
└──────────────┴──────────────────────────────────────────┴───────────────────┘
```

### 키프레임 생성 모달

```
┌─────────────────────────────────────────────────────────────────┐
│ ✨ Generate Keyframe                                       [X]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 📝 프롬프트 입력                                                │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ A country music artist standing in a golden wheat field    │ │
│ │ at sunset, wearing a denim jacket, looking hopeful...      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ 🎨 스타일 참조 (선택)                                           │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                                │
│ │ [+] │ │ img │ │ img │ │ img │                                │
│ │ Add │ │  1  │ │  2  │ │  3  │                                │
│ └─────┘ └─────┘ └─────┘ └─────┘                                │
│                                                                 │
│ 📦 굿즈 포함 (선택)                                             │
│ ┌─────┐ ┌─────┐                                                │
│ │앨범 │ │포토 │ □ 자연스럽게 배치                               │
│ │커버 │ │카드 │ □ 손에 들고 있게                                │
│ └─────┘ └─────┘                                                │
│                                                                 │
│ ⚙️ 생성 옵션                                                   │
│ • 이미지 비율: [9:16 ▼]  • 스타일 강도: [████████░░] 80%       │
│                                                                 │
│              [Cancel]              [⚡ Generate 4 Variations]   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 데이터베이스 스키마

### Prisma Schema 확장

```prisma
// 스토리보드 - 비디오 프로젝트의 컨테이너
model Storyboard {
  id              String            @id @default(uuid())
  campaignId      String            @map("campaign_id")
  name            String
  description     String?
  status          StoryboardStatus  @default(DRAFT)

  // 타임라인 설정
  totalDuration   Int               @default(15) @map("total_duration") // 초
  aspectRatio     String            @default("9:16") @map("aspect_ratio")

  // 오디오 설정
  audioAssetId    String?           @map("audio_asset_id")
  audioStartTime  Float             @default(0) @map("audio_start_time")
  audioVolume     Float             @default(1.0) @map("audio_volume")

  // 최종 결과물
  finalVideoId    String?           @map("final_video_id")

  // 관계
  campaign        Campaign          @relation(fields: [campaignId], references: [id])
  keyframes       Keyframe[]
  segments        VideoSegment[]

  // 메타데이터
  createdBy       String            @map("created_by")
  createdAt       DateTime          @default(now()) @map("created_at")
  updatedAt       DateTime          @updatedAt @map("updated_at")

  @@map("storyboards")
}

enum StoryboardStatus {
  DRAFT           // 초안
  KEYFRAMES_READY // 키프레임 완성
  GENERATING      // 세그먼트 생성 중
  COMPOSING       // 조합 중
  COMPLETED       // 완료
  FAILED          // 실패
}

// 키프레임 - 스토리보드의 주요 장면
model Keyframe {
  id              String            @id @default(uuid())
  storyboardId    String            @map("storyboard_id")

  // 위치 및 타이밍
  position        Float             // 0.0 ~ 1.0 (타임라인 상 위치)
  timestamp       Float             // 실제 시간 (초)
  holdDuration    Float             @default(0) @map("hold_duration") // 정지 시간

  // 이미지 소스
  generationType  KeyframeGenType   @map("generation_type")
  imageAssetId    String?           @map("image_asset_id")
  prompt          String?           // AI 생성 시 사용된 프롬프트

  // 스타일 참조
  styleReferenceIds String[]        @map("style_reference_ids")
  merchandiseIds    String[]        @map("merchandise_ids")

  // 트랜지션 설정
  transitionType  TransitionType    @default(CROSSFADE) @map("transition_type")
  transitionDuration Float          @default(0.5) @map("transition_duration")

  // 관계
  storyboard      Storyboard        @relation(fields: [storyboardId], references: [id], onDelete: Cascade)
  outgoingSegment VideoSegment?     @relation("SegmentStart")
  incomingSegment VideoSegment?     @relation("SegmentEnd")

  // 메타데이터
  createdAt       DateTime          @default(now()) @map("created_at")
  updatedAt       DateTime          @updatedAt @map("updated_at")

  @@map("keyframes")
}

enum KeyframeGenType {
  AI_GENERATED    // Imagen 3로 생성
  UPLOADED        // 사용자 업로드
  ASSET_LOCKER    // Asset Locker에서 선택
}

enum TransitionType {
  CUT             // 컷
  CROSSFADE       // 크로스페이드
  DISSOLVE        // 디졸브
  WIPE            // 와이프
  ZOOM            // 줌 트랜지션
}

// 비디오 세그먼트 - 키프레임 간 생성된 비디오
model VideoSegment {
  id              String            @id @default(uuid())
  storyboardId    String            @map("storyboard_id")

  // 키프레임 연결
  startKeyframeId String            @unique @map("start_keyframe_id")
  endKeyframeId   String?           @unique @map("end_keyframe_id") // null이면 hold

  // 생성 상태
  status          SegmentStatus     @default(PENDING)
  generationId    String?           @map("generation_id") // VEO generation 참조
  videoAssetId    String?           @map("video_asset_id")

  // 세그먼트 정보
  duration        Float             // 생성된 비디오 길이
  prompt          String?           // I2V 생성 시 사용된 프롬프트

  // 관계
  storyboard      Storyboard        @relation(fields: [storyboardId], references: [id], onDelete: Cascade)
  startKeyframe   Keyframe          @relation("SegmentStart", fields: [startKeyframeId], references: [id])
  endKeyframe     Keyframe?         @relation("SegmentEnd", fields: [endKeyframeId], references: [id])

  // 메타데이터
  createdAt       DateTime          @default(now()) @map("created_at")
  updatedAt       DateTime          @updatedAt @map("updated_at")

  @@map("video_segments")
}

enum SegmentStatus {
  PENDING         // 대기 중
  GENERATING      // 생성 중
  COMPLETED       // 완료
  FAILED          // 실패
}
```

---

## API 설계

### Storyboard APIs

```typescript
// POST /api/v1/campaigns/{campaignId}/storyboards
// 새 스토리보드 생성
interface CreateStoryboardRequest {
  name: string;
  description?: string;
  total_duration?: number;  // 기본 15초
  aspect_ratio?: string;    // 기본 9:16
  audio_asset_id?: string;
}

// GET /api/v1/campaigns/{campaignId}/storyboards
// 캠페인의 스토리보드 목록
interface StoryboardListResponse {
  items: Storyboard[];
  total: number;
  page: number;
  page_size: number;
}

// GET /api/v1/storyboards/{id}
// 스토리보드 상세 (키프레임, 세그먼트 포함)
interface StoryboardDetailResponse {
  storyboard: Storyboard;
  keyframes: Keyframe[];
  segments: VideoSegment[];
  preview_url?: string;
}
```

### Keyframe APIs

```typescript
// POST /api/v1/storyboards/{id}/keyframes
// 키프레임 추가
interface AddKeyframeRequest {
  position: number;           // 0.0 ~ 1.0
  generation_type: 'AI_GENERATED' | 'UPLOADED' | 'ASSET_LOCKER';
  image_asset_id?: string;    // ASSET_LOCKER 타입 시
  prompt?: string;            // AI_GENERATED 타입 시
  style_reference_ids?: string[];
  merchandise_ids?: string[];
  transition_type?: string;
}

// POST /api/v1/keyframes/{id}/generate
// AI로 키프레임 이미지 생성
interface GenerateKeyframeRequest {
  prompt: string;
  style_reference_ids?: string[];
  merchandise_ids?: string[];
  num_variations?: number;    // 기본 4
  style_strength?: number;    // 0.0 ~ 1.0
}

interface GenerateKeyframeResponse {
  variations: Array<{
    id: string;
    image_url: string;
    prompt_used: string;
  }>;
}

// POST /api/v1/keyframes/{id}/select-variation
// 생성된 변형 중 하나 선택
interface SelectVariationRequest {
  variation_id: string;
}
```

### Segment Generation APIs

```typescript
// POST /api/v1/storyboards/{id}/segments/generate
// 모든 세그먼트 생성 시작
interface GenerateSegmentsRequest {
  segments?: string[];        // 특정 세그먼트만 (생략 시 전체)
  motion_prompt?: string;     // 공통 모션 프롬프트
}

interface GenerateSegmentsResponse {
  batch_id: string;
  segments: Array<{
    segment_id: string;
    status: string;
  }>;
}

// GET /api/v1/storyboards/{id}/segments/status
// 세그먼트 생성 상태 확인
interface SegmentsStatusResponse {
  overall_status: string;
  progress: number;           // 0 ~ 100
  segments: Array<{
    segment_id: string;
    status: string;
    progress: number;
    video_url?: string;
    error?: string;
  }>;
}
```

### Composition APIs

```typescript
// POST /api/v1/storyboards/{id}/compose
// 최종 비디오 조합
interface ComposeRequest {
  audio_asset_id?: string;
  audio_start_time?: number;
  audio_volume?: number;
  fade_in?: number;
  fade_out?: number;
  output_quality?: 'draft' | 'final';
}

interface ComposeResponse {
  composition_id: string;
  status: string;
  estimated_time: number;     // 예상 소요 시간 (초)
}

// GET /api/v1/storyboards/{id}/compose/status
interface ComposeStatusResponse {
  status: 'PENDING' | 'PROCESSING' | 'COMPLETED' | 'FAILED';
  progress: number;
  output_url?: string;
  duration?: number;
  file_size?: number;
  error?: string;
}
```

---

## 구현 단계

### Phase 1: Foundation (1-2주)

**목표:** 기술 검증 및 기반 구축

```
Week 1:
├── Google Imagen 3 API 연동 (lib/imagen.ts)
│   ├── API 인증 설정
│   ├── 이미지 생성 함수 구현
│   ├── 스타일 참조 기능 테스트
│   └── 에러 핸들링
│
├── 데이터베이스 스키마 확장
│   ├── Storyboard, Keyframe, VideoSegment 모델 추가
│   ├── 마이그레이션 실행
│   └── Prisma 클라이언트 업데이트
│
└── VEO I2V 기능 강화 (lib/veo.ts)
    ├── referenceImageBase64 활용 최적화
    ├── 키프레임 기반 생성 테스트
    └── 품질 파라미터 조정

Week 2:
├── 기본 API 엔드포인트
│   ├── Storyboard CRUD
│   ├── Keyframe CRUD
│   └── 기본 생성 API
│
├── 스토리보드 매니저 (lib/storyboard-manager.ts)
│   ├── 스토리보드 생성/관리 로직
│   ├── 키프레임 정렬/관리
│   └── 상태 전이 관리
│
└── 기술 검증 테스트
    ├── Imagen → VEO 파이프라인 테스트
    ├── 스타일 일관성 테스트
    └── 성능 측정
```

### Phase 2: Storyboard UI (3-4주)

**목표:** 사용자 인터페이스 구축

```
Week 3:
├── 스토리보드 캔버스 컴포넌트
│   ├── components/storyboard/storyboard-canvas.tsx
│   ├── 타임라인 뷰 구현
│   ├── 드래그앤드롭 키프레임 배치
│   └── 줌/패닝 기능
│
├── 키프레임 카드 컴포넌트
│   ├── components/storyboard/keyframe-card.tsx
│   ├── 이미지 프리뷰
│   ├── 선택/편집 상태
│   └── 컨텍스트 메뉴
│
└── Asset Picker 연동
    ├── Asset Locker에서 이미지 선택
    ├── 굿즈 이미지 검색/선택
    └── 스타일 참조 선택

Week 4:
├── AI 키프레임 생성 UI
│   ├── 생성 모달 컴포넌트
│   ├── 프롬프트 입력
│   ├── 스타일 참조 선택
│   ├── 변형 미리보기
│   └── 선택/적용 플로우
│
├── 속성 패널 구현
│   ├── 키프레임 속성 편집
│   ├── 트랜지션 설정
│   └── 타이밍 조정
│
└── 스토리보드 목록/관리 페이지
    ├── app/(dashboard)/campaigns/[id]/storyboard/page.tsx
    ├── 스토리보드 목록 뷰
    └── 생성/삭제/복제 기능
```

### Phase 3: Video Segment Generation (5-6주)

**목표:** 세그먼트 생성 파이프라인 구축

```
Week 5:
├── 세그먼트 생성기 (lib/segment-generator.ts)
│   ├── 키프레임 쌍 → VEO I2V 호출
│   ├── 모션 프롬프트 생성
│   ├── 병렬 생성 관리
│   └── 재시도 로직
│
├── 생성 큐 시스템
│   ├── 대기열 관리
│   ├── 우선순위 처리
│   └── 리소스 제한 (동시 생성 수)
│
└── 진행 상태 추적
    ├── WebSocket 실시간 업데이트
    ├── 세그먼트별 진행률
    └── 에러 알림

Week 6:
├── 세그먼트 프리뷰 컴포넌트
│   ├── components/storyboard/segment-preview.tsx
│   ├── 비디오 미리보기 플레이어
│   ├── 세그먼트 연결 프리뷰
│   └── 개별 세그먼트 재생성
│
├── 품질 검증 시스템
│   ├── 자동 품질 점수
│   ├── 트랜지션 품질 체크
│   └── 권장 재생성 알림
│
└── 세그먼트 편집 기능
    ├── 트림 (시작/끝 조정)
    ├── 속도 조정
    └── 세그먼트 교체
```

### Phase 4: Composition Engine (7-8주)

**목표:** 최종 비디오 조합 시스템 구축

```
Week 7:
├── 비디오 컴포저 (lib/video-composer.ts)
│   ├── FFmpeg 기반 비디오 병합
│   ├── 트랜지션 효과 적용
│   │   ├── 크로스페이드
│   │   ├── 디졸브
│   │   ├── 와이프
│   │   └── 줌 트랜지션
│   └── 오디오 믹싱
│
├── 오디오 처리
│   ├── Asset Locker 오디오 연동
│   ├── 페이드 인/아웃
│   ├── 볼륨 조정
│   └── 동기화
│
└── 출력 관리
    ├── 다중 품질 옵션 (draft/final)
    ├── S3 업로드
    └── 메타데이터 저장

Week 8:
├── 조합 프리뷰 시스템
│   ├── 실시간 프리뷰 (저해상도)
│   ├── 타임라인 스크러빙
│   └── A/B 비교 뷰
│
├── 최종 출력 UI
│   ├── 조합 진행 상태 표시
│   ├── 품질 선택 옵션
│   └── 다운로드/공유 기능
│
└── 에러 복구 시스템
    ├── 부분 실패 처리
    ├── 체크포인트 저장
    └── 재시도 로직
```

### Phase 5: Polish & Integration (9-10주)

**목표:** 통합 및 최적화

```
Week 9:
├── The Bridge 통합
│   ├── Bridge → Storyboard 플로우
│   ├── 트렌드 키워드 전달
│   └── 자동 키프레임 제안
│
├── 기존 Generate 플로우 연동
│   ├── 단순 생성 vs 스토리보드 선택
│   ├── 스토리보드 결과 → 기존 시스템 저장
│   └── Curation/Analytics 연동
│
└── 품질 스코어링 연동
    ├── 스토리보드 전체 점수
    ├── 키프레임별 점수
    └── 개선 제안

Week 10:
├── 성능 최적화
│   ├── 이미지/비디오 캐싱
│   ├── 지연 로딩
│   └── 메모리 관리
│
├── 사용자 테스트 및 피드백
│   ├── 내부 테스트
│   ├── 피드백 수집
│   └── 우선순위 버그 수정
│
└── 문서화 및 배포
    ├── 사용자 가이드
    ├── API 문서
    └── 프로덕션 배포
```

---

## 기술적 고려사항

### 1. 스타일 일관성 유지

**문제:** AI 생성 키프레임 간 스타일 불일치
**해결:**
```typescript
interface StyleLock {
  colorPalette: string[];      // 고정 색상 팔레트
  lightingStyle: string;       // 조명 스타일
  cameraAngle: string;         // 카메라 앵글
  styleEmbedding?: string;     // Imagen 스타일 임베딩
}

// 스토리보드 생성 시 스타일 잠금 설정
// 모든 키프레임 생성에 동일 스타일 적용
```

### 2. 트랜지션 품질

**문제:** 세그먼트 간 부자연스러운 연결
**해결:**
```typescript
// 오버랩 생성 방식
const generateSegment = async (keyframeA: Keyframe, keyframeB: Keyframe) => {
  // 세그먼트 끝 1초를 다음 세그먼트 시작 1초와 오버랩
  const overlap = 1.0; // 초

  // VEO에 연속성 힌트 제공
  const motionPrompt = `Smooth transition from scene A to scene B,
    maintaining consistent lighting and camera movement`;

  return veoGenerateI2V({
    startImage: keyframeA.image,
    endImage: keyframeB.image,
    duration: segmentDuration + overlap,
    motionPrompt
  });
};
```

### 3. 유명인 이름 제한 우회

**문제:** Imagen도 실제 인물 생성 제한
**해결:**
```typescript
// 아티스트 사진은 "스타일 참조"로만 사용
// 생성되는 이미지는 "유사한 분위기의 익명 인물"

const generateKeyframePrompt = (concept: string, artistStyle: Asset[]) => {
  return {
    prompt: `${concept}, anonymous performer,
      similar aesthetic to reference images`,
    styleReferences: artistStyle,  // 스타일만 참조
    negativePrompt: "specific person, celebrity, named individual"
  };
};

// 굿즈는 직접 포함 가능 (상품이므로 제한 없음)
```

### 4. 처리 시간 최적화

**문제:** 다중 API 호출로 인한 긴 대기 시간
**해결:**
```typescript
// 병렬 처리 + 프로그레시브 로딩

// 1. 키프레임 병렬 생성
const keyframePromises = keyframes.map(kf => generateKeyframe(kf));
await Promise.all(keyframePromises);

// 2. 세그먼트 병렬 생성 (최대 3개 동시)
const segmentQueue = new PQueue({ concurrency: 3 });
for (const segment of segments) {
  segmentQueue.add(() => generateSegment(segment));
}

// 3. 완료된 세그먼트부터 프리뷰 표시
segmentQueue.on('completed', (segment) => {
  emitProgressUpdate(segment);
});
```

---

## 성공 지표

### 정량적 지표
| 지표 | 현재 | 목표 |
|------|------|------|
| 원하는 결과까지 재생성 횟수 | 5-10회 | 1-2회 |
| 비디오 생성 만족도 | 60% | 85%+ |
| 평균 프로젝트 완료 시간 | 2시간 | 45분 |
| 생성된 비디오 승인율 | 40% | 70%+ |

### 정성적 지표
- 사용자가 "예측 가능하다"고 느끼는가
- 키프레임 수정으로 원하는 방향 조정이 가능한가
- 전체 워크플로우가 직관적인가

---

## 리스크 및 대응

| 리스크 | 가능성 | 영향 | 대응 방안 |
|--------|--------|------|-----------|
| Imagen API 접근 제한 | 중 | 높음 | Stability AI 또는 자체 모델 대안 준비 |
| VEO I2V 품질 불안정 | 중 | 중 | 품질 검증 단계 추가, 자동 재생성 |
| 처리 비용 증가 | 높음 | 중 | 사용량 제한, 최적화, 캐싱 |
| 스타일 일관성 실패 | 중 | 중 | 스타일 잠금 기능, 수동 조정 옵션 |

---

## 다음 단계

### 즉시 실행 가능한 작업
1. Google Imagen 3 API 접근 권한 확인
2. VEO I2V 기능 상세 테스트 (현재 코드 기반)
3. 프로토타입 UI 와이어프레임 상세화

### 결정 필요 사항
1. MVP 범위 확정 (키프레임 3개 vs 5개)
2. 우선순위: 품질 vs 속도
3. 오디오 기능 포함 여부 (Phase 1)

---

*이 문서는 Visual Storyboard Studio 프로젝트의 초기 계획서입니다.*
*개발 진행에 따라 업데이트될 예정입니다.*

**작성일:** 2024-11-28
**버전:** 1.0
