# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["python311", "ffmpeg", "libGL", "glib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[variables]
PYTHONUNBUFFERED = "1"
